apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crypto-visualizer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "crypto-visualizer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crypto-visualizer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
